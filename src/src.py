print("delete this file later")


"""
add subfolders for the following:

app/
  -> app.py (UI)
  -> template_embeddings.py (builds the final prompt and put everything together)
  -> llm file (final output by llm)

ingestion/
  -> (add code for document parsing)

chunking/
  -> (add code for chunking and embedding)

finetuning-llm/
  -> (add code for finetuning llm) 
"""
